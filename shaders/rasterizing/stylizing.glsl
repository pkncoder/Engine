const float alphaCuttoff = 0.2;

const float shadowDiskRadius = 0.05;

const float exposure = 0.15;

const bool toneMap = true;

const vec3  fogColor = vec3(0.1, 0.1, 0.1);
const float fogDensity = 0.015;
const bool fog = true;

const float vignetteRadius = 1.5;
const float vignetteSoftness = 0.9;
const bool vignette = true;

